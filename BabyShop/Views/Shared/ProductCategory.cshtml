@model List<Model.EF.CategoryProduct>
@{
    var categoryProduct = (List<Model.EF.Product>)ViewBag.CategoryProduct;
    var category = (List<Model.EF.Category>)ViewBag.Category;
    var categorys = (Model.EF.Product)ViewBag.Categorys;
}
    <ul class="dropdown-menu">
        @foreach (var item in Model.Where(x => x.ParentID == 2))
        {
            <li><a href="/san-pham/@item.MetaTitle-@item.ID">@item.Name</a></li>
        }   
    </ul>